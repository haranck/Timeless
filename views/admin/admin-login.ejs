<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background: linear-gradient(rgba(0, 0, 0, -0.25), rgba(0, 0, 0, -1.15)), url("https://images.unsplash.com/photo-1578998323870-83a9a3d609e5?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D");
            background-repeat: no-repeat;
            background-size: cover;
            background-position: center;
        }
        .login-container {
            background-color: rgb(255, 255, 255, 0.1);
            border-radius: 0.75rem;
            color: #fff;
            width: 340px;
            padding: 2rem;
            margin-right: 594px;
        }
        .watch-image {
            width: 150px;
            height: 150px;
            margin-bottom: 1.5rem;
            border-radius: 50%;
            object-fit: cover;
        }
        h1 {
            color: #777272;
            margin-bottom: 1.5rem;
            font-size: 2rem;
            margin-left:35px
        }
        .input-group {
            margin-bottom: 1.2rem;
        }
        input {
            width: 100%;
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
            transition: border-color 0.3s;
            outline: none;
        }
        input:focus {
            border-color: #667eea;
        }
        .login-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            cursor: pointer;
            width: 100%;
            transition: transform 0.2s;
        }
        .login-btn:hover {
            transform: translateY(-2px);
        }
        .email, .password {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="login-container">
        
        <h1>Admin Login</h1>
        <% if (typeof message !== 'undefined') { %>
            <div style="color: red; margin-bottom: 1rem; text-align: center;;"> <%= message %> </div>
        <% } %>
        <form action="/admin/login" method="POST">
            <div class="input-group">
                <label for="email" class="email">Email</label>
                <input type="email" placeholder="Email Address" name="email" >
            </div>
            <div class="input-group">
                <label for="password" class="password">Password</label>
                <input type="password" placeholder="Password" name="password" >
            </div>
            <button type="submit" class="login-btn">Login</button>
        </form>
    </div>
</body>
</html>
